# ============================================================
# Supabase Configuration
# ============================================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY is NOT required by this template.
# Only add it if you build server-side admin features that need to bypass RLS
# (e.g. background jobs, webhooks, admin dashboards).
# Never expose this key to the browser.

# ============================================================
# App Mode Configuration
# ============================================================
# WORKSPACE_MODE: solo | teams
#   solo  - single user, no invites, personal org only
#   teams - multi-user, full invite + RBAC support
NEXT_PUBLIC_WORKSPACE_MODE=teams

# PROJECT_MODE: single | multi
#   single - one hidden default project per org (project UI invisible)
#   multi  - full project management UI visible
NEXT_PUBLIC_PROJECT_MODE=single

# ============================================================
# App URL (used for invite links and OAuth redirects)
# ============================================================
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================
# Email (Resend) — required for invite emails
# ============================================================
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM_EMAIL=noreply@yourdomain.com
RESEND_FROM_NAME=YourAppName
